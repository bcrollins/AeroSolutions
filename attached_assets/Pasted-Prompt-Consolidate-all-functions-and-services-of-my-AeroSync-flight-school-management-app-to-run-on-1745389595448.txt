Prompt: Consolidate all functions and services of my AeroSync flight school management app to run on a single port (0.0.0.0:8080), map it to external port 80, delete the other ports (0.0.0.0:5000 and 0.0.0.0:8081), and ensure full functionality across all features. My app uses React, TypeScript, Express.js, and PostgreSQL, targeting flight school administrators, instructors, students, mechanics, and front desk staff. Follow these steps:





Analyze Current Port Usage:





Identify all services running on ports 0.0.0.0:5000, 0.0.0.0:8080, and 0.0.0.0:8081 by checking server code (e.g., /server/index.js, /server/app.js) and other scripts.



Determine if ports 5000 and 8081 are running critical services (e.g., WebSocket, dev server). If yes, integrate their functionality into the main Express.js server on port 8080. If not, mark them for removal.



Confirm the main Express.js server is running on port 8080, as it should be the primary service.



Update Server Code to Use Port 8080:





Modify the main Express.js server (e.g., /server/index.js) to listen on 0.0.0.0:8080. Example: const express = require('express'); const app = express(); app.get('/', (req, res) => res.send('AeroSync Flight School App')); app.listen(8080, '0.0.0.0', () => console.log('Server running on http://0.0.0.0:8080'));



If other services (e.g., WebSocket on port 8081) exist, integrate them into the Express.js app. Example for WebSocket: const WebSocket = require('ws'); const server = app.listen(8080, '0.0.0.0'); const wss = new WebSocket.Server({ server }); wss.on('connection', ws => { ws.on('message', message => console.log('Received:', message)); ws.send('Connected to AeroSync WebSocket'); });



If port 5000 is a dev server (e.g., Vite for React), reconfigure the front-end to proxy requests to the Express.js server on port 8080. Update vite.config.ts: server: { proxy: { '/api': 'http://localhost:8080' } }



Verify PostgreSQL Connectivity:





Ensure the PostgreSQL database connection isn’t affected by port changes. Use connection details from .env: DB_HOST=your_postgres_host DB_PORT=5432 DB_NAME=aerosync_db DB_USER=your_user DB_PASSWORD=your_password



Test connectivity in Express.js: const { Pool } = require('pg'); require('dotenv').config(); const pool = new Pool({ host: process.env.DB_HOST, port: process.env.DB_PORT, database: process.env.DB_NAME, user: process.env.DB_USER, password: process.env.DB_PASSWORD, }); app.get('/test-db', async (req, res) => { const result = await pool.query('SELECT NOW()'); res.json(result.rows); });



Update .replit File:





Modify the .replit file to run the main server: run = "node server/index.js"



Ensure no other scripts are running on ports 5000 or 8081.



Update Replit Ports Configuration:





In the Replit Ports panel, set 0.0.0.0:8080 to map to external port 80.



Delete the port mappings for 0.0.0.0:5000 and 0.0.0.0:8081.



Test Full Functionality:





Verify all features work on the new port:





Front-end: Access React app at https://..repl.co (should load without specifying a port).



Back-end: Test API endpoints (e.g., GET /flights) via https://..repl.co/flights.



Database: Ensure PostgreSQL queries (e.g., /test-db) return results.



Other services: If WebSocket or other features were on port 8081, confirm they work on port 8080.



Check console logs for errors (e.g., port conflicts, missing dependencies).



If the app doesn’t load, refresh the webview, clear browser cache, or fork the Repl.



Debug and Finalize:





If issues arise, create a minimal test app to isolate problems: const http = require('http'); http.createServer((req, res) => { res.write('Test page'); res.end(); }).listen(8080, '0.0.0.0');



Ensure all routes, UI components, and database interactions work as expected for AeroSync’s users (admins, instructors, students, mechanics, front desk staff).

Output all changes to existing files (e.g., /server/index.js, vite.config.ts, .replit) and confirm the app runs on 0.0.0.0:8080, mapped to external port 80, with no other ports active.